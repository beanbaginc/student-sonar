Student Sonar
=============

Getting set up:
```
$ npm install -g jspm
$ npm install -g nodemon
$ npm install
$ jspm install
```

To run:
```
$ nodemon
```


TODO
----
* Use `History.pushState()` and some client-side routing to make the back
  button work
